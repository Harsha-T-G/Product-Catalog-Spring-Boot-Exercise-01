spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/product_catalog}
    username: ${DB_USERNAME:product_catalog_app}
    password: ${DB_PASSWORD:}

catalog:
  low-stock-threshold: 10
  maximum-products: 1000
